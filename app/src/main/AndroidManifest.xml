<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VehicleRepairTracker"
        tools:targetApi="31">
        <activity
            android:name=".UI.LoginActivity"
            android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
        <activity
            android:name=".UI.RepairDetailsActivity"
            android:exported="false"
            android:label="Repair Details"
            android:parentActivityName=".UI.CarDetailsActivity" />
        <activity
            android:name=".UI.CarDetailsActivity"
            android:exported="false"
            android:label="Vehicle Details"
            android:parentActivityName=".UI.CarActivity" />
        <activity
            android:name=".UI.CarActivity"
            android:exported="false"
            android:label="Vehicles"
            android:parentActivityName=".UI.MainActivity" />
        <activity
            android:name=".UI.MainActivity"
            android:exported="false"
            android:label="Vehicle Repair Tracker">

        </activity>
    </application>

</manifest>